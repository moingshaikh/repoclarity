# RepoClarity

**Understand any repo, locally.**

RepoClarity is a local-first CLI tool that helps developers quickly understand any codebase using a local LLM. Pass a local path or a GitHub URL and get a structured breakdown instantly.

No API keys. No cloud. No data leaving your machine.

---

## The problem

Opening a new repo is slow.

- README is incomplete
- Codebase is unfamiliar
- You don't know where to start

You spend 20-30 minutes just figuring out:

- what this repo does
- which files matter
- whether it's worth your time

---

## What RepoClarity does

RepoClarity analyzes a repository and returns a structured breakdown:

- what the repo does
- key files to look at
- important insights
- risks or limitations
- suggested next steps

All in one response. All on your machine.

---

## Usage

```bash
# Analyze a local repo
rclr ./path/to/repo "what does this repo do"

# Analyze a GitHub repo directly
rclr https://github.com/owner/repo "what does this repo do"

# Analyze a GitHub repo and keep the local clone
rclr https://github.com/owner/repo "what does this repo do" --keep
```

---

## Example

```bash
rclr https://github.com/fastapi/fastapi "what does this repo do?"
```

Output:

```json
{
  "repo_summary": "A FastAPI project with automated workflows and documentation.",
  "what_it_does": "This repository contains a FastAPI application with various workflows for testing, building, and deploying. It also includes documentation in multiple languages.",
  "key_files": [
    "README.md",
    "docs_src/app_testing/app_a_py310/main.py",
    ".github/workflows/build-docs.yml",
    ".github/workflows/pre-commit.yml",
    "docs/de/docs/tutorial/security/first-steps.md"
  ],
  "insights": [
    "The repository uses GitHub Actions for automated testing and documentation building.",
    "Documentation is available in multiple languages, including German.",
    "There are workflows that do not perform any actions but are used for branch protection."
  ],
  "risks": [
    "Branch protection workflows that do nothing could be redundant or a mistake.",
    "The use of hardcoded tokens like `fake_secret_token` should be reconsidered for security reasons."
  ],
  "suggested_actions": [
    "Review the branch protection workflows to ensure they are necessary and functional.",
    "Consider using environment variables or secrets management services for sensitive information like tokens.",
    "Expand documentation coverage or improve existing sections as needed."
  ]
}
```

<img width="1393" height="645" alt="demo" src="https://github.com/user-attachments/assets/5385f61a-d891-427e-b7c8-71f7acd55498" />

Real output generated by RepoClarity on the FastAPI GitHub repo. Runs locally on a 10-year-old ThinkPad, CPU-only, no GPU.

---

## Why local-first matters

- no API costs
- no rate limits
- works offline
- safe for private codebases

---

## How it works

- scans key files in the repo
- builds a constrained context
- sends it to a local LLM (via Ollama)
- returns structured output

When given a GitHub URL, RepoClarity clones the repo to a temporary directory, analyzes it, and deletes it automatically. Use `--keep` to retain the clone.

---

## Requirements

- Python 3.12+
- Ollama installed
- Git installed (required for GitHub URL support)
- Model:

```bash
ollama pull qwen2.5-coder
```

---

## Install

```bash
git clone https://github.com/moingshaikh/repoclarity.git
cd repoclarity
pip install -e .
```

---

## Scope (v1)

RepoClarity v1 focuses on:

- fast repo understanding
- local execution
- structured output
- GitHub URL support with auto-clone

Not included yet:

- deep code analysis
- multi-file reasoning chains
- agent workflows

---

## Why this exists

Most AI tooling today assumes:

- cloud access
- API keys
- external inference

RepoClarity explores a different direction:

> What if understanding codebases could happen entirely locally?

---

## License

MIT

---

Built by [Moin Shaikh](https://www.linkedin.com/in/moingshaikh/)
